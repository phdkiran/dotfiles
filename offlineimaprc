[general]
pythonfile = ~/.offlineimap.py
accounts = FastMail
# ui = blinkenlights 
ui = ttyui 
socktimeout = 60
maxsyncaccounts = 3

[Account FastMail]
localrepository = Local
remoterepository = Remote
status_backend = sqlite
autorefresh = 2
postsynchook = notmuch new

[Repository Local]
localfolders = ~/Mail
type = Maildir
readonly = False

[Repository Remote]
type = IMAP
remotehost = mail.messagingengine.com
remoteport = 993
ssl = yes
# https://www.fastmail.com/certs/?domain=fastmail.fm
sslcacertfile = ~/Repositories/romanzolotarev/dotfiles/DigiCertHighAssuranceEVRootCA.crt
cert_fingerprint = 958b1601563aef92607a41eb5bad22e3cace8431
holdconnectionopen = yes
maxconnections = 3
readonly = False
remoteusereval = get_imap_email()
remotepasseval = get_imap_password()
